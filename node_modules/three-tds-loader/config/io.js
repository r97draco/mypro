'use strict'

const { paths: { src, dist } } = require('./paths')

const entry = {
  "three.tds-loader": ['babel-polyfill', src('index')]
}
const filename = process.env.NODE_ENV === "PROD" ? "[name].min.js" : "[name].js";
const output = {
  path: dist(),
  filename: filename
}

module.exports = { entry, output }
